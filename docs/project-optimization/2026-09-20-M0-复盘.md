# M0 复盘（Walking Skeleton）

> 日期：2026-09-20（M0批5 机器侧收官时点）｜ 定位：差距定级 → 优化点分级 → 否决分析 → M1 批次建议，经用户定案后细化 M1 卷，不代改设计。
> 状态：**待用户定案**（E2E-M0-1 回传后合并实证读数终审）。

## 1. 差距定级（M0 DoD 对照）

| DoD | 状态 | 差距 |
|---|---|---|
| 1. Widget→SSE 全链通，trace/指标可见 | 机器侧 ✅（mock 模型 38 测试）；trace/指标实际出数 = E2E-M0-1 用户侧 | 真实模型首响/整轮时延读数待回传 |
| 2. ArchUnit/测试门禁 CI 强制 | ✅（本地）；CI 依赖远端仓库启用（M0批1 已注记） | 远端 push 后生效 |
| 3. V-01~V-03 核验结论回写 | ✅（《01》v1.2.0） | — |
| 4. E2E-M0-1 用户回传销账 | ⬜ 待执行（含 E2E-M0-4） | 阻塞 M0 正式收官 |
| 5. 复盘提案入 project-optimization | ✅ 本文 | 待定案 |

## 2. 优化点提案（P0 > P1 > P2）

| # | 级 | 提案 | 依据（实证） | 建议归属 |
|---|---|---|---|---|
| O-1 | **P0** | **生产 RLS 接线**：应用以 `cs_app` 连接 + 每事务 `SET LOCAL cs.tenant_id` 注入（Hibernate 拦截器/事务同步器），补集成测试 | V2 迁移已建角色但应用未接线（dev/test 超管旁路）；上线前不接 = RLS 形同虚设（M0批2 已知缺口） | M1批1（会话状态机批次，同触碰会话域） |
| O-2 | P1 | 同会话并发轮锁（`cs:lock:turn:{sessionId}` + TURN_IN_PROGRESS）+ 合并队列窗口 | 《03》§5 设计项未落：连发消息可致 cs_turn seq 竞态（唯一键冲突未覆盖 seq+1 读改写窗口） | M1批1 |
| O-3 | P1 | 首响指标 `cs_chat_first_token_seconds`（TurnFrames 增 firstTokenLatency 回传） | 《10》验收 P95≤2s 依赖此指标，当前缺（M0批5 注记） | M1批2（模型路由批次，同改 TurnFrames） |
| O-4 | P1 | cs_session_event/message 月分区（pg_partman 或原生）+ streams 表随会话 CLOSED 清理 | 《12》§2 分区策略挂起项；总线 streams 内存随会话数增长（M0批5 注记） | M1批1（事件量起势后） |
| O-5 | P1 | 采样矩阵落地（默认 20%、工具/审批/ERROR 100%，OTel sampler tailor） | M0=1.0 全量仅验收期用（《10》§4） | M1批6（观测深化，同 Langfuse 接入） |
| O-6 | P2 | `/actuator/prometheus` basic auth 化（Prometheus scrape credentials） | 当前 permitAll 内网口径（《11》v1.3.1 注记） | M1 安全复审 |
| O-7 | P2 | 测试镜像钉版：pgvector/pg17 浮动 tag 钉实 tag；心跳 200ms 用例与 ChatFlow 合并瘦身 | M0批2 遗留；编排链测试 4 用例各起容器拖慢 CI | M1批1 顺带 |
| O-8 | P2 | turn state/token 指标常量与事件类型字面量收敛（CsConstants 扩 event 域） | M0批4/5 出现跨类字面量（"AI"/"USER"/"ROUTE_DECIDED"）分散 | M1批1 顺带 |
| O-9 | P2 | CI 增加 `-Dtest` 失败重跑与 Docker daemon 预检 | 本地 Docker Desktop 空退致构建失败三连（M0批4/5 实证） | 工程顺带 |

## 3. 否决分析（本阶段不做及理由）

- **SSE 补发缓冲 Redis 化**：D-M0-1 定案维持内存至多实例批次（单实例收益为零、引入跨实例序号一致性问题）。
- **TurnPlan/意图路由/护栏链提前**：属 M1 设计节奏，M0 最小链验证目标已达成，提前引入无增量验证价值。
- **A2A/MCP 集成**：V-01 已核验留档，M1批4 按知识接入节奏走，不前移。

## 4. M1 批次建议（供定案）

以 M1 卷既有规划为主轴，建议插入/强调：M1批1 吸收 O-1/O-2/O-4/O-7/O-8；M1批2 吸收 O-3；M1批6 吸收 O-5/O-6。E2E-M0-1 回传若暴露真实模型链路问题（首响超限/丢帧），升 P0 优先处置。
