# 15 · 附录：实证坑位台账

> 最后更新:2026-09-20 · v1.5.0(坑#23/#24 续编：M0批5 实证——总线 sink 终结语义/Boot 4.1 OTLP 属性前缀) · v1.4.0(坑#20~#22 续编：M0批4 实证——Spring AI 2.0 OpenAI 模块 API 剧变/SSE 总线丢帧双缺陷/-pl 旧构件陷阱) · v1.3.0(坑#19：INF-2 Casdoor 实测——roles 对象数组/JWKS 端点路径/Security 7 因子 authority) · v1.2.0(坑#15~#18 续编：M0批3 实证——Jackson3 坐标/WebTestClient 变形/Sink 双发/PG 事务毒化) · v1.1.0(坑#13/#14) · v1.0.0(初版)

## 1. 机制说明

- **编号**：`坑#NN` 纯文本，连续递增、只增不改（不使用带圈数字——50 号封顶且跳号要永久解释，姊妹项目实证教训）。
- **模板**：`**病名（症状+根因一句话）**——机制解释——治法（具体代码/配置）——（触发阶段/轮次、联动章节版本号）`。
- **四点联动**（登记一个坑必须同步四处）：
  1. 本台账续编（新行）；
  2. 对应设计章修订注记（`vN.M(坑#xx 续编:…)`，章头版本链同步）；
  3. AGENTS.md「坑位钩子」仅加一句话版（全量台账在本文件，AGENTS.md 控体积）；
  4. 提交信息带坑号（`fix(域): …（坑#xx）`）+ 对应 M 卷任务行/热修行补记。
- 姊妹项目全量台账（①-㊿，50 条）见其仓库 `docs/project-implement/19-附录.md` 附录 E——本文件仅收录与本平台技术面相关的条目作**预置继承**。

## 2. 预置继承坑位（来源：kb-rag-agent 附录 E，2026-09-20 提炼）

| # | 坑位 | 对本平台的适用点 |
|---|---|---|
| 坑#01 | **MCP server 缺省装配 SSE 端点，`/mcp` 404**——Spring AI 服务端 `protocol` 未显式设置时默认 SSE——显式 `spring.ai.mcp.server.protocol: STREAMABLE` | M3 平台 MCP server 暴露时（D-16）；集成文档已警示 |
| 坑#02 | **a2a-java SDK 桥接与 Spring 栈不适配（Quarkus/CDI 绑定）**——姊妹项目 spike 判负回落自研协议层 | D-06/V-04：自研预案权重上调，M2 D-M2-1 决策依据 |
| 坑#03 | **Spring AI 2.0.1 升级三迁移（Redis 记忆 autoconfigure 包/类/配置前缀）**——跨小版本升级需源码 diff 核验 | BOM 升级（2.0.x→2.1）时的回归清单 |
| 坑#04 | **容器内多 ExecutorService/ChatClient Bean 按类型注入歧义，条件装配首次开启即启动失败**——单测手工装配不走容器解析故全绿——消费点显式 `@Qualifier` + 反射契约测试防回退 | cs-ai-core 多 ChatClient 装配（RoutingChatModel）落地时 |
| 坑#05 | **yml 显式配置段遮蔽 Java 缺省值**——门禁/阈值类配置「看起来设了其实没生效」——配置结构与缺省值用契约测试钉死 | 评测门禁、路由阈值、护栏开关类配置 |
| 坑#06 | **监控镜像 latest 标签漂移（2.x→3.x 行为变化致整次抓取作废）**——全部镜像钉版本号 | INF-1/INF-5 compose 资产纪律 |
| 坑#07 | **promtool 跨版本序列语义漂移致单测 FAIL**——告警规则单测按钉版 promtool 实测修订 | 《10》§7 告警规则 CI 化时 |
| 坑#08 | **AppCDS 消费侧 SIGSEGV 崩溃循环**——生产 JVM 优化择机，不默认启用 | 部署形态（D-02 单部署） |
| 坑#09 | **分栈 compose `--remove-orphans` 互杀**——多 compose 栈共存时全栈单入口 include 化 + 单 systemd unit | 本平台与 KB 栈同 ECS 共存（端口 8100/监控栈段） |
| 坑#10 | **同 ECS 多栈端口冲突**（9090/3001 等常见端口已占）——部署前端口预检清单 | INF-1 端口分配表前置 |
| 坑#11 | **配置段误挂错误节点从不生效**（如记忆配置挂到 tools 节点）——关键配置启动时打生效值日志 + 契约测试 | Advisor/模型/护栏配置装配 |
| 坑#12 | **前端构建期 env 内联（VITE_* 类）**——生产域名后置变更需重构建，运行时 env 不生效 | Web Widget 前端资产交付形态 |

## 3. 本平台实证坑位（首坑即启）

| # | 坑位 |
|---|---|
| 坑#13 | **Testcontainers 2.x 三处 API 形态变化，按 1.x 记忆写码必错**——artifact 改名（`testcontainers-postgresql`/`testcontainers-junit-jupiter`，旧 `postgresql`/`junit-jupiter` 在 BOM 中无版本致 Maven 解析失败）；`PostgreSQLContainer` 迁包 `org.testcontainers.postgresql`；类已非泛型（`<?>` 编译错）——依姊妹项目 2.0.5 同栈写法核验后落码（M0批2，V1 样板即 ConversationPersistenceIntegrationTest） |
| 坑#14 | **嵌套 JdbcTemplate 不保证与外层 ConnectionCallback 同连接——SET ROLE/SET LOCAL 场景 GUC 与数据语句分家**——JPA 事务管理器对 JDBC 连接暴露时机的坑：外层回调里 `SET LOCAL cs.tenant_id` 生效的连接 ≠ 内层 `jdbc.update` 借到的连接，RLS fail-closed 直接拒绝（表象是「本租户写入也被拒」）——会话级 GUC/角色场景必须**同一条 Connection 内完成全部语句**（PreparedStatement 手工执行），finally 中 RESET ROLE 在 abort 事务上必 25P02 需吞掉保原始异常（M0批2） |
| 坑#15 | **Boot 4 = Jackson 3（`tools.jackson`），starter-web 不再携带 `com.fasterxml.jackson`**——按 2.x 习惯 import `com.fasterxml.jackson.databind.ObjectMapper` 直接编译失败；手工构造用 `JsonMapper`，且 `readTree` 等方法签名微调（M0批3） |
| 坑#16 | **Framework 7 WebTestClient API 变形**——`retrieve()` 改名 `exchange()`、`exchangeToMono` 不存在、包路径仍为 `org.springframework.test.web.reactive.server`；SSE 流测试改用普通 `WebClient` + `spring-boot-starter-webflux`(test) 更稳（姊妹同款组合）（M0批3） |
| 坑#17 | **`Sinks.many().multicast().onBackpressureBuffer()` 自带历史回放**——与手工补发缓冲 concat 后每帧双发（测试见 12 帧应为 6）；补发场景应 `directBestEffort`（热流不存历史，缓冲为补发唯一来源）（M0批3） |
| 坑#18 | **PG 唯一键冲突后事务即毒化（25P02），catch 后同事务内的任何补偿 SQL 必失败**——幂等兜底正确结构：冲突异常滚出事务边界，补偿读走**新事务**（ChatService 捕获 DataIntegrityViolation 后经 ConversationPort.inboundMessageId 独立事务读取）（M0批3） |
| 坑#19 | **Casdoor JWT 实测契约与假设不符三处**——① `roles` 为**对象数组**，角色名在元素内 `name` 字段（**顶层 `name` 是用户名**，两处同名字段极易混用）；② JWKS 端点 = `/.well-known/jwks`（**无 `.json` 后缀**，带后缀路由返回空 `{"keys":[]}`；以 OIDC discovery 为权威）；③ `/api/jwt/public-key`、`/api/jwks` 需管理鉴权不可匿名用——外部身份契约必须以真实 token + discovery 实测定型再落码（INF-2 回传 2026-09-20 实证：RS256 验签 openssl 独立通过、`owner`=组织即租户、`aud`=`{client_id}-org-{org}`、TTL 7 天）；治法 = SecurityConfig.roleName 按 `roles[].name` 提取并兼容字符串形态 + AuthorityConverterTest 镜像真实 payload 钉死；另附 Framework 7 / Security 7 实测：bearer 认证默认追加 `FACTOR_BEARER` authority，测试精确断言须过滤角色面（INF-2 销账批 2026-09-20，《11》§7 v1.3.0） |
| 坑#20 | **Spring AI 2.0 OpenAI 模块 API 相对 1.x 全面剧变**——① `org.springframework.ai.openai.api.OpenAiApi` 类已不存在（底层换官方 openai-java SDK 的 `OpenAIClient`）；② `OpenAiChatModel.builder()` 无 `openAiApi/defaultOptions`，改为 `.options(OpenAiChatOptions)` 且 **baseUrl/apiKey 经 options 传入**（异步 client 不继承预建同步 client 凭证，姊妹坑位同源）；③ `ChatModel` 已合并 `StreamingChatModel`（实现 call+stream 即得全量面）；④ `ChatResponse.builder()` 必须显式 `.generations(...)` 否则 build 即抛 IllegalArgumentException；⑤ 手工模型必须显式挂 `ObservationRegistry`（builder 缺省 NOOP，观测断链）——纪律 6 源码级核验（.m2 jar javap）+ 姊妹项目 SmartRoutingConfig 实证拦截，未成事故（M0批4，《04》§8 v1.1.0） |
| 坑#21 | **SSE 帧总线两处丢帧缺陷（M0批3 潜伏，M0批4 集成测试实证暴露）**——① 无轮次会话订阅返回 `Flux.empty()`：客户端「先订阅后发消息」的正确时序下连接即关、EventSource 陷入重连循环——修为 `computeIfAbsent` 建流挂起；② 补发快照与热订阅挂接之间存在缝隙：窗口内发射的帧既不在重放快照、又因 `directBestEffort` 无订阅者被丢——修为 `Flux.create` 桥接（buffer 锁内先重放再挂热订阅、按 id 去重，快照与订阅原子化）。附协议语义澄清：**DONE/ERROR 即完成流、一轮一连接**，多轮经 Last-Event-ID 重连续接；echo 桩因帧间延迟掩盖 ①②，快引擎（mock/真实模型毫秒级出帧）才暴露（M0批4，《08》§8 v1.2.0） |
| 坑#22 | **`mvn -pl <module>` 单测跑在本地仓库旧构件上**——未 `install` 过/或本仓库有旧版本时，兄弟模块依赖取 `.m2` 打包产物而非工作区源码：对兄弟模块的修复不进测试类路径，出现「改了没生效/时灵时不灵」的迷惑现象（Maven 自身已警告 `...more recent than the packaged artifact...please run a full build`）——单模块快测必须 `-pl <module> -am` 或全量 reactor（M0批4 实证：总线修复在 `-pl cs-api` 下无效、`-am` 后立绿） |
| 坑#23 | **会话帧总线的 sink 不可随 DONE/ERROR 终结（M0批5 真实事故）**——`sinkFor().done()` 里 `tryEmitComplete()` 把**会话级** sink 终结后，第二轮起全部实时帧 `tryEmitNext` 静默失败（只进补发缓冲），任何在线订阅者收不到帧且连接永不完成（叠加心跳 merge 时表现为 20s 挂死超时）——正确语义切分：sink = 会话生命周期热总线（不终结），「DONE/ERROR 即完成流」是**单订阅连接**的语义，由桥接层转发到终止帧时 complete 该订阅 emitter（M0批5，SessionFrameBus；坑#21 的续深修复） |
| 坑#24 | **Boot 4.1 OTLP 导出属性前缀迁移（姊妹实证预防）**——endpoint 键为 `management.otlp.tracing.export.otlp.endpoint`（Boot 4.1 迁移后形态，非旧版 `management.otlp.export.tracing.endpoint`），且该值为**信号专属 endpoint**：OTel SDK 原样使用不自动追加 `/v1/traces`，必须带全路径，缺路径段对端 404；导出总开关 `management.tracing.export.otlp.enabled` 缺省关——未配端点时零导出零噪音（M0批5 采纳姊妹实证形态，《10》§8 v1.1.0） |

## 4. AGENTS.md 坑位钩子（一句话版，高频项）

- 坑#01：MCP server 暴露必须显式 `protocol: STREAMABLE`。
- 坑#02：a2a-java SDK 有 Spring 适配判负先例，A2A server 优先自研预案。
- 坑#04：多 ChatClient/ExecutorService Bean 消费点必须显式 `@Qualifier`。
- 坑#05/坑#11：关键配置必须契约测试钉结构与缺省值。
- 坑#13：Testcontainers 2.x artifact 已改名（testcontainers-postgresql / testcontainers-junit-jupiter）、PostgreSQLContainer 迁包 `org.testcontainers.postgresql` 且非泛型。
- 坑#15：Boot 4 = Jackson 3（`tools.jackson.*`），`com.fasterxml.jackson.databind` 不在 starter-web 类路径。
- 坑#19：Casdoor `roles` 为对象数组、角色名取元素内 `name`（顶层 `name`=用户名勿混用）；JWKS 端点以 OIDC discovery 为准（`/.well-known/jwks`，无 `.json` 后缀）。
- 坑#20：Spring AI 2.0 无 `OpenAiApi`——`OpenAiChatModel.builder().options(...)` 且 baseUrl/apiKey 经 `OpenAiChatOptions` 传入；`ChatResponse.builder()` 必须显式 generations。
- 坑#21：SSE 总线订阅必须 `computeIfAbsent` + 快照/热订阅锁内桥接；DONE 即完成流、一轮一连接、多轮经 Last-Event-ID 续接。
- 坑#22：`-pl` 单模块快测必须带 `-am`，否则兄弟模块跑本地仓库旧构件。
- 坑#23：会话帧总线 sink 不随 DONE 终结——终结是单订阅连接的语义（桥接层负责）。
- 坑#24：Boot 4.1 OTLP endpoint 键 `management.otlp.tracing.export.otlp.endpoint` 且须带全路径 `/v1/traces`。
